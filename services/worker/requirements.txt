# Web framework -- worker exposes /health and /metrics endpoints
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# AWS SDK -- for SQS polling
boto3==1.35.36

# Metrics
prometheus-client==0.21.0

# Datadog APM -- worker uses native ddtrace instead of OTel SDK
# This gives us both instrumentation approaches to talk about in interviews
ddtrace==2.11.0

# Testing
pytest==8.3.3
httpx==0.27.2
